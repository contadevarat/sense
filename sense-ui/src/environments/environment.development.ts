export const environment = {
  production: false,
  // Empty during local dev -> the app uses the browser-local repository.
  // Point this at a local/staging API (e.g. http://localhost:3000/api)
  // once a backend is available, to exercise the HTTP repository instead.
  apiBaseUrl: 'http://127.0.0.1:8000',
};
